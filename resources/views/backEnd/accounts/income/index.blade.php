@extends('backEnd.layouts.master')
@section('title','Income Ledger')
@section('content')
<div class="container-fluid"><div class="row"><div class="col-12"><div class="page-title-box"><div class="page-title-right"><a href="{{ route('accounts.income.create') }}" class="btn btn-primary rounded-pill">Create</a></div><h4 class="page-title">Income Ledger</h4></div></div></div><div class="card"><div class="card-body"><form class="mb-3"><div class="row g-3"><div class="col-md-4"><input type="text" class="form-control" name="keyword" value="{{ request('keyword') }}" placeholder="Income name"></div><div class="col-md-4"><select class="form-control" name="income_category_id"><option value="">All Categories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected((string) request('income_category_id') === (string) $category->id)>{{ $category->name }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-primary w-100">Filter</button></div></div></form><table class="table table-striped align-middle"><thead><tr><th>Name</th><th>Category</th><th>Financial</th><th>Amount</th><th>Date</th><th>Status</th><th>Action</th></tr></thead><tbody>@foreach($data as $value)<tr><td>{{ $value->name }}@if($value->note)<div class="small text-muted">{{ $value->note }}</div>@endif</td><td>{{ $value->category?->name ?: '-' }}</td><td><div>{{ $value->financialAccount?->name ?: '-' }}</div><div class="small text-muted">{{ $value->accountHead?->name ?: '' }}</div></td><td>{{ number_format((float)$value->amount,2,'.','') }}</td><td>{{ $value->received_at?->format('d M Y') ?: '-' }}</td><td>{!! $value->status ? '<span class="badge bg-success">Active</span>' : '<span class="badge bg-danger">Inactive</span>' !!}</td><td><a href="{{ route('accounts.income.edit', $value->id) }}" class="btn btn-primary btn-sm">Edit</a></td></tr>@endforeach</tbody></table>{{ $data->links('pagination::bootstrap-4') }}</div></div></div>
@endsection
