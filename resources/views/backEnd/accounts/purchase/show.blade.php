@extends('backEnd.layouts.master')
@section('title','Purchase Details')
@section('content')
<div class="container-fluid"><div class="row"><div class="col-12"><div class="page-title-box"><div class="page-title-right"><a href="{{ route('accounts.purchases.edit', $purchase->id) }}" class="btn btn-warning rounded-pill me-2">Edit</a><a href="{{ route('accounts.purchases.return.create', $purchase->id) }}" class="btn btn-danger rounded-pill me-2">Purchase Return</a><a href="{{ route('accounts.supplier_payments.create') }}" class="btn btn-success rounded-pill me-2">Supplier Payment</a><a href="{{ route('accounts.purchases.index') }}" class="btn btn-primary rounded-pill">Manage</a></div><h4 class="page-title">Purchase Details</h4></div></div></div><div class="row"><div class="col-lg-4"><div class="card"><div class="card-body"><h5 class="mb-3">{{ $purchase->purchase_no }}</h5><p class="mb-1"><strong>Supplier:</strong> {{ $purchase->supplier_name }}</p><p class="mb-1"><strong>Date:</strong> {{ $purchase->purchase_date?->format('d M Y') ?: '-' }}</p><p class="mb-1"><strong>Reference:</strong> {{ $purchase->reference_no ?: '-' }}</p><p class="mb-1"><strong>Account:</strong> {{ $purchase->financialAccount?->name ?: '-' }}</p><p class="mb-0"><strong>Note:</strong> {{ $purchase->note ?: '-' }}</p></div></div><div class="card"><div class="card-body"><div class="d-flex justify-content-between"><span>Subtotal</span><strong>{{ number_format((float) $purchase->subtotal, 2, '.', '') }}</strong></div><div class="d-flex justify-content-between"><span>Discount</span><strong>{{ number_format((float) $purchase->discount_amount, 2, '.', '') }}</strong></div><div class="d-flex justify-content-between"><span>Transport</span><strong>{{ number_format((float) $purchase->transport_cost, 2, '.', '') }}</strong></div><div class="d-flex justify-content-between"><span>Other Cost</span><strong>{{ number_format((float) $purchase->other_cost, 2, '.', '') }}</strong></div><div class="d-flex justify-content-between"><span>Grand Total</span><strong>{{ number_format((float) $purchase->grand_total, 2, '.', '') }}</strong></div><div class="d-flex justify-content-between"><span>Paid</span><strong>{{ number_format((float) $purchase->paid_amount, 2, '.', '') }}</strong></div><div class="d-flex justify-content-between"><span>Due</span><strong>{{ number_format((float) $purchase->due_amount, 2, '.', '') }}</strong></div></div></div></div><div class="card"><div class="card-body"><h5 class="mb-3">Payment Summary</h5><div class="d-flex justify-content-between"><span>Total Payments</span><strong>{{ number_format((float) $purchase->payments->sum('amount'), 2, '.', '') }}</strong></div><div class="d-flex justify-content-between"><span>Total Returns</span><strong>{{ number_format((float) $purchase->returns->sum('total_amount'), 2, '.', '') }}</strong></div><div class="d-flex justify-content-between"><span>Outstanding Due</span><strong>{{ number_format((float) $purchase->due_amount, 2, '.', '') }}</strong></div></div></div></div><div class="col-lg-8"><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-striped align-middle"><thead><tr><th>Product</th><th>Variant</th><th>Qty</th><th>Unit Cost</th><th>Total</th></tr></thead><tbody>@foreach($purchase->items as $item)<tr><td>{{ $item->product_name }}</td><td>{{ $item->variant_name ?: '-' }}</td><td>{{ $item->qty }}</td><td>{{ number_format((float) $item->unit_cost, 2, '.', '') }}</td><td>{{ number_format((float) $item->line_total, 2, '.', '') }}</td></tr>@endforeach</tbody></table></div></div></div><div class="card"><div class="card-body"><h5 class="mb-3">Returns</h5><table class="table table-sm"><thead><tr><th>No</th><th>Date</th><th>Total</th></tr></thead><tbody>@forelse($purchase->returns as $return)<tr><td>{{ $return->return_no }}</td><td>{{ $return->return_date?->format('d M Y') ?: '-' }}</td><td>{{ number_format((float) $return->total_amount, 2, '.', '') }}</td></tr>@empty<tr><td colspan="3" class="text-center">No return yet.</td></tr>@endforelse</tbody></table><h5 class="mb-3 mt-4">Payment History</h5><table class="table table-sm"><thead><tr><th>Date</th><th>Amount</th><th>Account</th><th>Ref</th></tr></thead><tbody>@forelse($purchase->payments as $payment)<tr><td>{{ $payment->payment_date?->format('d M Y') ?: '-' }}</td><td>{{ number_format((float) $payment->amount, 2, '.', '') }}</td><td>{{ $payment->financialAccount?->name ?: '-' }}</td><td>{{ $payment->reference_no ?: '-' }}</td></tr>@empty<tr><td colspan="4" class="text-center">No supplier payment yet.</td></tr>@endforelse</tbody></table></div></div></div></div></div>
@endsection
