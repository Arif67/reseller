@extends('backEnd.layouts.master')
@section('title','Bonus Manage')
@section('content')
<div class="container-fluid"><div class="row"><div class="col-12"><div class="page-title-box"><div class="page-title-right"><a href="{{ route('hr.bonuses.create') }}" class="btn btn-primary rounded-pill">Create</a></div><h4 class="page-title">Bonus / Incentive Manage</h4></div></div></div><div class="card"><div class="card-body"><form class="mb-3"><div class="row g-3"><div class="col-md-4"><select class="form-control" name="employee_id"><option value="">All Employees</option>@foreach($employees as $employee)<option value="{{ $employee->id }}" @selected((string) request('employee_id') === (string) $employee->id)>{{ $employee->name }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-primary w-100">Filter</button></div></div></form><table class="table table-striped align-middle"><thead><tr><th>Employee</th><th>Title</th><th>Type</th><th>Amount</th><th>Status</th><th>Action</th></tr></thead><tbody>@foreach($data as $value)<tr><td>{{ $value->employee?->name }}</td><td>{{ $value->title }}</td><td>{{ $value->bonus_type ?: '-' }}</td><td>{{ number_format((float) $value->amount, 2, '.', '') }}</td><td>{!! $value->status ? '<span class="badge bg-success">Active</span>' : '<span class="badge bg-danger">Inactive</span>' !!}</td><td class="d-flex gap-1">@if($value->status)<form method="post" action="{{ route('hr.bonuses.inactive') }}">@csrf<input type="hidden" name="hidden_id" value="{{ $value->id }}"><button class="btn btn-secondary btn-sm">Inactive</button></form>@else<form method="post" action="{{ route('hr.bonuses.active') }}">@csrf<input type="hidden" name="hidden_id" value="{{ $value->id }}"><button class="btn btn-success btn-sm">Active</button></form>@endif<a href="{{ route('hr.bonuses.edit', $value->id) }}" class="btn btn-primary btn-sm">Edit</a><form method="post" action="{{ route('hr.bonuses.destroy') }}">@csrf<input type="hidden" name="hidden_id" value="{{ $value->id }}"><button class="btn btn-danger btn-sm">Delete</button></form></td></tr>@endforeach</tbody></table>{{ $data->links('pagination::bootstrap-4') }}</div></div></div>
@endsection
