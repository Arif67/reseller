@extends('backEnd.layouts.master')
@section('title','Document Manage')
@section('content')
<div class="container-fluid"><div class="row"><div class="col-12"><div class="page-title-box"><div class="page-title-right"><a href="{{ route('hr.documents.create') }}" class="btn btn-primary rounded-pill">Create</a></div><h4 class="page-title">Document Manage</h4></div></div></div><div class="card"><div class="card-body"><form class="mb-3"><div class="row g-3"><div class="col-md-4"><select class="form-control" name="employee_id"><option value="">All Employees</option>@foreach($employees as $employee)<option value="{{ $employee->id }}" @selected((string) request('employee_id') === (string) $employee->id)>{{ $employee->name }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-primary w-100">Filter</button></div></div></form><table class="table table-striped align-middle"><thead><tr><th>Employee</th><th>Title</th><th>Type</th><th>File</th><th>Status</th><th>Action</th></tr></thead><tbody>@foreach($data as $value)@php($fileUrl = $value->file_path ? (str_starts_with($value->file_path, 'http') ? $value->file_path : asset($value->file_path)) : null)@php($extension = $value->file_path ? strtoupper(pathinfo(parse_url($value->file_path, PHP_URL_PATH) ?: $value->file_path, PATHINFO_EXTENSION)) : null)<tr><td>{{ $value->employee?->name }}</td><td>{{ $value->title }}</td><td>{{ $value->document_type ?: '-' }}</td><td>@if($fileUrl)<span class="badge bg-light text-dark me-2">{{ $extension ?: 'FILE' }}</span><a href="{{ $fileUrl }}" target="_blank" class="btn btn-sm btn-outline-primary me-1">Open</a><a href="{{ $fileUrl }}" download class="btn btn-sm btn-outline-secondary">Download</a>@else - @endif</td><td>{!! $value->status ? '<span class="badge bg-success">Active</span>' : '<span class="badge bg-danger">Inactive</span>' !!}</td><td class="d-flex gap-1">@if($value->status)<form method="post" action="{{ route('hr.documents.inactive') }}">@csrf<input type="hidden" name="hidden_id" value="{{ $value->id }}"><button class="btn btn-secondary btn-sm">Inactive</button></form>@else<form method="post" action="{{ route('hr.documents.active') }}">@csrf<input type="hidden" name="hidden_id" value="{{ $value->id }}"><button class="btn btn-success btn-sm">Active</button></form>@endif<a href="{{ route('hr.documents.edit', $value->id) }}" class="btn btn-primary btn-sm">Edit</a><form method="post" action="{{ route('hr.documents.destroy') }}">@csrf<input type="hidden" name="hidden_id" value="{{ $value->id }}"><button class="btn btn-danger btn-sm">Delete</button></form></td></tr>@endforeach</tbody></table>{{ $data->links('pagination::bootstrap-4') }}</div></div></div>
@endsection
