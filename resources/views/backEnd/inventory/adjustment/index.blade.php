@extends('backEnd.layouts.master')
@section('title','Stock Adjustment')
@section('content')
<div class="container-fluid"><div class="row"><div class="col-12"><div class="page-title-box"><div class="page-title-right"><a href="{{ route('inventory.adjustments.create') }}" class="btn btn-primary rounded-pill">Create Adjustment</a></div><h4 class="page-title">Stock Adjustment</h4></div></div></div><div class="card"><div class="card-body"><div class="table-responsive"><table class="table table-striped align-middle"><thead><tr><th>Date</th><th>Product</th><th>Variant</th><th>Type</th><th>Qty</th><th>Reason</th></tr></thead><tbody>@forelse($data as $value)<tr><td>{{ $value->adjustment_date?->format('d M Y') ?: '-' }}</td><td>{{ $value->product?->name ?: '-' }}</td><td>{{ $value->productVariable ? trim(collect([$value->productVariable->size, $value->productVariable->color])->filter()->implode(' / ')) : '-' }}</td><td><span class="badge bg-info text-dark">{{ $value->adjustment_type }}</span></td><td>{{ $value->qty }}</td><td>{{ $value->reason ?: '-' }}</td></tr>@empty<tr><td colspan="6" class="text-center">No adjustment found.</td></tr>@endforelse</tbody></table></div>{{ $data->links('pagination::bootstrap-4') }}</div></div></div>
@endsection
