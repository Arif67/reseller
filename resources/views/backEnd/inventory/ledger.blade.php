@extends('backEnd.layouts.master')
@section('title','Inventory Ledger')
@section('content')
<div class="container-fluid"><div class="row"><div class="col-12"><div class="page-title-box"><h4 class="page-title">Inventory Ledger</h4></div></div></div><div class="card"><div class="card-body"><form class="row g-3 mb-3"><div class="col-md-3"><input type="text" class="form-control" name="keyword" value="{{ request('keyword') }}" placeholder="Reference / note"></div><div class="col-md-3"><select class="form-control" name="movement_type"><option value="">All movement</option><option value="purchase_in" @selected(request('movement_type')==='purchase_in')>Purchase In</option><option value="sale_out" @selected(request('movement_type')==='sale_out')>Sale Out</option><option value="adjustment_in" @selected(request('movement_type')==='adjustment_in')>Adjustment In</option><option value="adjustment_out" @selected(request('movement_type')==='adjustment_out')>Adjustment Out</option></select></div><div class="col-md-2"><input type="date" class="form-control" name="date_from" value="{{ request('date_from') }}"></div><div class="col-md-2"><input type="date" class="form-control" name="date_to" value="{{ request('date_to') }}"></div><div class="col-md-2"><button class="btn btn-primary w-100">Filter</button></div></form><div class="table-responsive"><table class="table table-striped align-middle"><thead><tr><th>Date</th><th>Product</th><th>Variant</th><th>Type</th><th>Qty</th><th>Before</th><th>After</th><th>Reference</th></tr></thead><tbody>@forelse($data as $value)<tr><td>{{ $value->movement_date?->format('d M Y') ?: '-' }}</td><td>{{ $value->product?->name ?: '-' }}</td><td>{{ $value->productVariable ? trim(collect([$value->productVariable->size, $value->productVariable->color])->filter()->implode(' / ')) : '-' }}</td><td><span class="badge bg-secondary">{{ $value->movement_type }}</span></td><td>{{ $value->direction === 'out' ? '-' : '+' }}{{ $value->qty }}</td><td>{{ $value->stock_before }}</td><td>{{ $value->stock_after }}</td><td>{{ $value->reference_no ?: '-' }}</td></tr>@empty<tr><td colspan="8" class="text-center">No inventory movement found.</td></tr>@endforelse</tbody></table></div>{{ $data->links('pagination::bootstrap-4') }}</div></div></div>
@endsection
